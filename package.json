{
  "name": "@j3ssie/pi-loop",
  "version": "0.1.1",
  "description": "A /loop slash command for the Pi coding agent — re-run a prompt on an interval inside the current session (agentic cron).",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi",
    "pi-coding-agent",
    "extension",
    "loop",
    "scheduler",
    "cron"
  ],
  "pi": {
    "extensions": [
      "dist/index.js"
    ]
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target node --format esm --external @earendil-works/pi-coding-agent",
    "typecheck": "tsc -p tsconfig.json",
    "test": "bun ./test/engine.mjs && bun ./test/slash.mjs && bun ./test/widget.mjs && bun ./test/filemodes.mjs && bun ./test/parse.mjs && bun ./test/sentinel.mjs && bun ./test/persist.mjs && bun ./test/features.mjs && bun ./test/improvements.mjs",
    "dev": "pi -e ./src/index.ts",
    "npm-publish": "bun run scripts/npm-publish.ts",
    "npm-publish:dry": "PILOOP_NPM_DRY_RUN=1 bun run scripts/npm-publish.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.79.0"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.79.0",
    "@types/node": "^25.9.3",
    "typescript": "^5.6.0"
  }
}
